# Only these 2 env vars are required at deploy time.
# All other credentials are managed via the dashboard (Settings → API Credentials).
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres
CREDENTIAL_ENCRYPTION_KEY=your-32-char-encryption-key-here!
